import geoWithin from "@turf/boolean-within";
import { Point } from "@turf/helpers";

export const pointWithin = (point, targetArea) => {
    // const pt = Point(point.coordinates);
  //   const isWithin = geoWithin(pt, targetArea);
  //   return isWithin;
  console.log(point);
};
